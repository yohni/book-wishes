import './App.css';

function App({ contract, nearConfig }) {
  console.log(nearConfig);
  return (
    <div className="App">
      <button className="btn btn-primary">Hallo</button>
    </div>
  );
}

export default App;
